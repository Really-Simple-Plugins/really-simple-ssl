import {create} from 'zustand';
import {produce} from "immer";
import * as rsssl_api from "../../utils/api";

/*
    This class fetches all values from the forced field and stores them in a set.
 */
const useForcedRolesStore = create(( set, get ) => ({

}));
export default useForcedRolesStore;